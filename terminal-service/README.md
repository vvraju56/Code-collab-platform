# terminal-service

Phase 1 monorepo slot for the PTY terminal sharing service (node-pty + WebSocket fanout).

Current implementation lives in `services/executor/` under the `/terminals/:projectId` WebSocket endpoint and is runnable via root `npm run dev`.

