# execution-service

Phase 1 monorepo slot for the Docker sandbox runner (multi-language code execution).

Currently scaffolded as part of `services/executor/` (HTTP `/execute` stub). Full Docker sandbox is implemented in Phase 7.

